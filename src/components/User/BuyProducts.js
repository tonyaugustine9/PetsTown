import { Box, Typography } from "@mui/material";

const BuyProducts = () => {
  return <Box>
      <Typography>
          In Buy products page
      </Typography>
  </Box>
};

export default BuyProducts;
